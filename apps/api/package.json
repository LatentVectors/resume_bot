{
  "name": "api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": ".venv/bin/uvicorn api.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'Python API does not require build step'",
    "lint": ".venv/bin/ruff check .",
    "test": ".venv/bin/pytest",
    "setup": "python3 -m venv .venv && .venv/bin/pip install --upgrade pip && .venv/bin/pip install -e '.[dev]'"
  }
}

